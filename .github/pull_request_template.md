**What changed and why**

**How it was tested** (which suite, which new test)

**Config language changed?** no / yes — config language change documented in docs/CONFIG.md and CHANGELOG

**Benchmark before/after** (only for `http1` / `server` changes)

- [ ] `gofmt -l .` is empty and `go vet ./...` is clean
- [ ] No new dependencies (go.mod stays stdlib-only)
